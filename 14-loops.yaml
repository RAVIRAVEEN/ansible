- name: loops 
  hosts: localhost
  become: yes
  tasks:
  - name: install or uninstall packages
    ansible.builtin.package:
       name: "{{item}}" #default key word
       state: present
    loop:
    - git
    - postfix
    - mysql
     